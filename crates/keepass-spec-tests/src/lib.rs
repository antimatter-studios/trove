//! Test-only host crate. See [`tests/`](../tests/) for the actual spec suite.
//!
//! This file exists because cargo requires a library or binary target in
//! every package; we have neither, just integration tests under `tests/`.
//! Keeping it empty avoids polluting the public API of `keepass`.
